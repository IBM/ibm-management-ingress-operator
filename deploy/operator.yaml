apiVersion: apps/v1
kind: Deployment
metadata:
  name: management-ingress-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: management-ingress-operator
  template:
    metadata:
      labels:
        name: management-ingress-operator
    spec:
      serviceAccountName: management-ingress-operator
      containers:
        - name: management-ingress-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - management-ingress-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "management-ingress-operator"
